extends:
  - default

rules:
  line-length:
    max: 120
    level: warning
  indentation:
    spaces: 2
  comments:
    min-spaces-from-content: 1

ignore:
  - .github/ISSUE_TEMPLATE
  - tests/fixtures
  - action/Dockerfile
  - docker/Dockerfile
